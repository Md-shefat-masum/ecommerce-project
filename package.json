{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postcss:watch": "postcss assets/css/styles.css -o assets/css/styles.css -w"
  },
  "devDependencies": {
    "postcss-preset-env": "^7.8.2",
    "postcss": "^8.4.18",
    "postcss-cli": "^10.0.0"
  }
}
